# Streamlit Secrets Example
# Copy this file to secrets.toml and add your actual secrets

[general]
app_name = "CardioFusion"
version = "1.0.0"

# Add any API keys or secrets here if needed
# example_api_key = "your_key_here"
